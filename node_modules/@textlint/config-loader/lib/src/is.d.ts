import type { TextlintRuleModule } from "@textlint/types";
import { TextlintConfigRulePreset } from "./TextlintConfigDescriptor.js";
import { TextlintFilterRuleReporter } from "@textlint/types";
/**
 * detect that ruleCreator has linter function
 * @param {*} ruleCreator
 * @returns {boolean}
 */
export declare function hasLinter(ruleCreator: unknown): boolean;
/**
 * detect that ruleCreator has fixer function
 * @param {*} ruleCreator
 * @returns {boolean}
 */
export declare function hasFixer(ruleCreator: unknown): boolean;
/**
 * is textlint rule module
 * type guard
 * @param mod
 */
export declare const isTextlintRuleModule: (mod: unknown) => mod is TextlintRuleModule;
/**
 * is textlint filter rule reporter
 * type guard
 * @param mod
 */
export declare const isTextlintFilterRuleModule: (mod: unknown) => mod is TextlintFilterRuleReporter;
/**
 * is textlint rule preset
 * type guard
 * @param mod
 */
export declare const isTextlintRulePresetCreator: (mod: unknown) => mod is TextlintConfigRulePreset["preset"];
//# sourceMappingURL=is.d.ts.map